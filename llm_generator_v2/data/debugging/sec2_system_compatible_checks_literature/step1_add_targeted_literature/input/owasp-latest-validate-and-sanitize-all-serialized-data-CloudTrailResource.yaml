check:
  category: system_integrity
  control_names:
  - NIST-800-53-SI-10
  - ISO-27001-A.13.1.3
  literature: Serialized data, such as objects or data structures, can contain sensitive
    information or executable code. If this data is not properly validated and sanitized,
    it can lead to various security vulnerabilities like code injection, data tampering,
    or unauthorized access. This check ensures that all serialized data is thoroughly
    validated and sanitized before being processed or deserialized, mitigating the
    risk of attacks that exploit insecure deserialization vulnerabilities.
  provider: aws
  resource:
    field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - id
    - source_connector
    - trails
    - trails[]
    - trails[].enable_log_file_validation
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].id
    - trails[].include_global_service_events
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].kms_key_id
    - trails[].log_file_validation_enabled
    - trails[].name
    - trails[].s3_bucket_name
    - trails[].s3_key_prefix
    - trails[].tags
    - trails[].tags[]
    - trails[].tags[].key
    - trails[].tags[].value
    name: CloudTrailResource
  unique_id: owasp-latest-validate-and-sanitize-all-serialized-data
