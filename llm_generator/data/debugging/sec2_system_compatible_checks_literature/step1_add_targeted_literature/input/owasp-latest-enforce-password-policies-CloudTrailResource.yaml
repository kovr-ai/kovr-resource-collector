check:
  category: authentication
  control_names:
  - NIST-800-53-IA-5
  - ISO-27001-A.9.4.3
  literature: Password policies are a set of rules and requirements that govern the
    creation, usage, and management of passwords within an organization or system.
    These policies aim to enhance the security of authentication mechanisms by enforcing
    strong password practices, reducing the risk of password guessing or cracking
    attacks, and mitigating the potential impact of compromised credentials. Implementing
    robust password policies is crucial for protecting sensitive information, maintaining
    system integrity, and preventing unauthorized access. This security check validates
    the existence and enforcement of strong password policies, which typically include
    requirements such as minimum password length, complexity rules, password expiration
    periods, and restrictions on password reuse.
  provider: aws
  resource:
    field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - id
    - source_connector
    - trails
    - trails[]
    - trails[].enable_log_file_validation
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].id
    - trails[].include_global_service_events
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].kms_key_id
    - trails[].log_file_validation_enabled
    - trails[].name
    - trails[].s3_bucket_name
    - trails[].s3_key_prefix
    - trails[].tags
    - trails[].tags[]
    - trails[].tags[].key
    - trails[].tags[].value
    name: CloudTrailResource
  unique_id: owasp-latest-enforce-password-policies
