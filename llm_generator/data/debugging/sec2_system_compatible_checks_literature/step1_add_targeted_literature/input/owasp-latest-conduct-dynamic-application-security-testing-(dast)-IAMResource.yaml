check:
  category: system_integrity
  control_names:
  - NIST-800-53-RA-5
  - NIST-800-53-SI-2
  - ISO-27001-A.14.2.8
  literature: 'Dynamic Application Security Testing (DAST) is a technique used to
    identify security vulnerabilities in web applications by simulating real-world
    attacks and analyzing the application''s behavior and responses. DAST tools interact
    with the running application from the outside, mimicking the actions of a malicious
    user or attacker. This approach helps uncover vulnerabilities that may be missed
    by static code analysis or other testing methods.


    The importance of conducting DAST lies in its ability to detect vulnerabilities
    that can lead to data breaches, unauthorized access, or system compromise. By
    simulating real-world attacks, DAST tools can identify issues such as injection
    flaws, broken authentication, sensitive data exposure, XML external entities (XXE),
    and other security weaknesses that could be exploited by attackers.


    Regular DAST testing is crucial for organizations to ensure the security of their
    web applications, especially those handling sensitive data or providing critical
    services. It helps identify and mitigate vulnerabilities before they can be exploited,
    reducing the risk of data breaches, financial losses, and reputational damage.'
  provider: aws
  resource:
    field_paths:
    - id
    - policies
    - policies[]
    - policies[].attachment_count
    - policies[].create_date
    - policies[].default_version
    - policies[].default_version.CreateDate
    - policies[].default_version.Document
    - policies[].default_version.Document.Statement
    - policies[].default_version.Document.Statement[]
    - policies[].default_version.Document.Statement[].Action
    - policies[].default_version.Document.Statement[].Action[]
    - policies[].default_version.Document.Statement[].Condition
    - policies[].default_version.Document.Statement[].Effect
    - policies[].default_version.Document.Statement[].Resource
    - policies[].default_version.Document.Version
    - policies[].default_version.IsDefaultVersion
    - policies[].default_version.VersionId
    - policies[].default_version_id
    - policies[].id
    - policies[].path
    - policies[].policy_id
    - policies[].policy_name
    - policies[].update_date
    - source_connector
    - users
    - users[]
    - users[].access_keys
    - users[].access_keys[]
    - users[].access_keys[].create_date
    - users[].access_keys[].id
    - users[].access_keys[].last_used_date
    - users[].access_keys[].last_used_region
    - users[].access_keys[].last_used_service
    - users[].access_keys[].status
    - users[].arn
    - users[].create_date
    - users[].id
    - users[].mfa_devices
    - users[].mfa_devices[]
    - users[].mfa_devices[].enable_date
    - users[].mfa_devices[].serial_number
    - users[].mfa_devices[].type
    - users[].mfa_devices[].virtual_mfa_token
    - users[].path
    - users[].user_id
    name: IAMResource
  unique_id: owasp-latest-conduct-dynamic-application-security-testing-(dast)
