check:
  category: encryption
  control_names:
  - NIST-800-53-SC-28
  - ISO-27001-A.10.1.1
  literature: Data at rest refers to inactive data stored on any digital medium, such
    as hard drives, databases, cloud storage, or backup tapes. Implementing encryption
    for data at rest is a crucial security measure to protect sensitive information
    from unauthorized access, data breaches, and cyber threats. Unencrypted data at
    rest is vulnerable to theft, misuse, or exposure, which can lead to severe consequences
    like identity theft, financial loss, and regulatory non-compliance. Encrypting
    data at rest ensures that even if an attacker gains physical access to the storage
    medium, the data remains unintelligible and unusable without the proper decryption
    keys. This check validates that encryption is implemented for all data at rest,
    following industry-standard encryption algorithms and key management practices.
  provider: aws
  resource:
    field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - id
    - source_connector
    - trails
    - trails[]
    - trails[].enable_log_file_validation
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].id
    - trails[].include_global_service_events
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].kms_key_id
    - trails[].log_file_validation_enabled
    - trails[].name
    - trails[].s3_bucket_name
    - trails[].s3_key_prefix
    - trails[].tags
    - trails[].tags[]
    - trails[].tags[].key
    - trails[].tags[].value
    name: CloudTrailResource
  unique_id: owasp-latest-implement-encryption-for-data-at-rest
