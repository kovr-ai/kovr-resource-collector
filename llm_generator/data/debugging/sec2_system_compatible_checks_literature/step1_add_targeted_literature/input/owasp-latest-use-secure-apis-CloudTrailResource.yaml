check:
  category: system_integrity
  control_names:
  - NIST-800-53-SA-11
  - ISO-27001-A.14.2.1
  literature: The OWASP latest use secure APIs check validates that applications use
    the latest, secure versions of APIs and libraries to prevent vulnerabilities introduced
    by outdated or insecure code. Using outdated or insecure APIs can expose applications
    to known vulnerabilities, increasing the risk of data breaches, unauthorized access,
    and other security incidents. This check is important because it helps ensure
    that applications are built with secure and up-to-date components, reducing the
    attack surface and minimizing the risk of exploitation.
  provider: aws
  resource:
    field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - id
    - source_connector
    - trails
    - trails[]
    - trails[].enable_log_file_validation
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].id
    - trails[].include_global_service_events
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].kms_key_id
    - trails[].log_file_validation_enabled
    - trails[].name
    - trails[].s3_bucket_name
    - trails[].s3_key_prefix
    - trails[].tags
    - trails[].tags[]
    - trails[].tags[].key
    - trails[].tags[].value
    name: CloudTrailResource
  unique_id: owasp-latest-use-secure-apis
