check:
  category: system_integrity
  control_names:
  - NIST-800-53-SA-3
  - ISO-27001-A.14.2.1
  literature: The OWASP Secure Software Development Lifecycle (SSDLC) is a set of
    best practices and guidelines for building secure software from the ground up.
    It emphasizes the importance of incorporating security considerations throughout
    the entire software development process, from requirements gathering and design
    to implementation, testing, and deployment. By adopting an SSDLC, organizations
    can proactively address security risks and vulnerabilities, rather than reactively
    addressing them after the software is released. This check validates whether an
    organization has adopted and follows a secure SSDLC, which is crucial for developing
    secure and resilient applications that can withstand various cyber threats.
  provider: aws
  resource:
    field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - id
    - source_connector
    - trails
    - trails[]
    - trails[].enable_log_file_validation
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].id
    - trails[].include_global_service_events
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].kms_key_id
    - trails[].log_file_validation_enabled
    - trails[].name
    - trails[].s3_bucket_name
    - trails[].s3_key_prefix
    - trails[].tags
    - trails[].tags[]
    - trails[].tags[].key
    - trails[].tags[].value
    name: CloudTrailResource
  unique_id: owasp-latest-adopt-a-secure-software-development-lifecycle-(ssdlc)
