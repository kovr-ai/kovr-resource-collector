check:
  invalid_resources: []
  unique_id: owasp-latest-monitor-for-suspicious-activities
  valid_resources:
  - field_paths:
    - policies
    - policies[].default_version.Document.Statement
    - users[].access_keys
    - users[].mfa_devices
    literature: The IAMResource in AWS can be used to implement the 'owasp-latest-monitor-for-suspicious-activities'
      security check. By analyzing the IAM policies, user access keys, and MFA device
      information, you can monitor for suspicious activities such as unauthorized
      access attempts, privilege escalation, or misuse of credentials. The policies
      field can be inspected for overly permissive statements, while the user access
      keys and MFA devices can be checked for unusual activity patterns or disabled
      MFA.
    name: IAMResource
    reason: The IAMResource contains relevant information about IAM policies, user
      access keys, and MFA devices, which can be used to detect suspicious activities
      related to authentication, authorization, and access control.
  - field_paths:
    - event_selectors
    - event_selectors[]
    - event_selectors[].data_resources
    - event_selectors[].data_resources.type
    - event_selectors[].data_resources.values
    - event_selectors[].data_resources.values[]
    - event_selectors[].include_management_events
    - event_selectors[].read_write_type
    - trails
    - trails[]
    - trails[].event_selectors
    - trails[].event_selectors[]
    - trails[].event_selectors[].data_resources
    - trails[].event_selectors[].data_resources.type
    - trails[].event_selectors[].data_resources.values
    - trails[].event_selectors[].data_resources.values[]
    - trails[].event_selectors[].include_management_events
    - trails[].event_selectors[].read_write_type
    - trails[].insight_selectors
    - trails[].insight_selectors[]
    - trails[].insight_selectors[].insight_type
    - trails[].is_logging
    - trails[].is_multi_region_trail
    - trails[].log_file_validation_enabled
    literature: The AWS CloudTrail resource is highly applicable for implementing
      the 'owasp-latest-monitor-for-suspicious-activities' security check. CloudTrail
      is a service that records AWS API calls and related events, providing a comprehensive
      audit trail of activities across your AWS account. By configuring CloudTrail
      trails and event selectors, you can capture and monitor various types of events,
      including management events, data events, and insights events. This allows you
      to detect and investigate suspicious or anomalous activities within your AWS
      environment, such as unauthorized access attempts, policy changes, or data exfiltration.
    name: CloudTrailResource
    reason: The CloudTrail resource provides comprehensive logging and monitoring
      capabilities for AWS API calls and events, allowing you to detect and investigate
      suspicious or anomalous activities within your AWS environment. By configuring
      CloudTrail trails, event selectors, and insight selectors, you can capture and
      analyze various types of events, including management events, data events, and
      insights events, which are essential for implementing the 'owasp-latest-monitor-for-suspicious-activities'
      security check.
  - field_paths:
    - alarms
    - alarms[].alarm_actions
    - alarms[].alarm_description
    - alarms[].alarm_name
    - alarms[].comparison_operator
    - alarms[].dimensions
    - alarms[].dimensions[].Name
    - alarms[].dimensions[].Value
    - alarms[].evaluation_periods
    - alarms[].metric_name
    - alarms[].namespace
    - alarms[].period
    - alarms[].statistic
    - alarms[].threshold
    - log_groups
    - log_groups[].log_group_name
    - metrics
    - metrics[].dimensions
    - metrics[].dimensions[].Name
    - metrics[].dimensions[].Value
    - metrics[].metric_name
    - metrics[].namespace
    literature: The AWS CloudWatch service can be used to implement the 'owasp-latest-monitor-for-suspicious-activities'
      security check. CloudWatch allows you to collect and monitor various logs, metrics,
      and events from your AWS resources and applications. By creating CloudWatch
      Alarms and configuring appropriate alarm actions, you can detect and alert on
      suspicious or anomalous activities within your AWS environment.
    name: CloudWatchResource
    reason: CloudWatch Alarms can be configured to monitor various metrics and logs
      from AWS services and applications. By setting appropriate alarm thresholds
      and actions, you can detect and alert on suspicious activities, such as unusual
      resource usage patterns, unauthorized access attempts, or security-related events.
      Additionally, CloudWatch Log Groups can be used to collect and analyze log data
      from various sources, enabling you to identify and investigate potential security
      incidents.
